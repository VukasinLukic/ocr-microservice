# =================================================
# SV-20 OCR Mikroservis - Python Dependencies
# =================================================
# Instaliraj sa: pip install -r requirements.txt
# Kompatibilno sa Python 3.13
# =================================================

# Core Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# OCR Engine
easyocr>=1.7.1

# NAPOMENA (ANALIZA-OCR-SERVISA.md 4.3): onnxruntime-directml NE ubrzava
# EasyOCR - EasyOCR interno koristi PyTorch (torch.cuda), ne onnxruntime.
# Ovaj paket je ovde jer je originalno tako planirano, ali trenutno ne radi
# ništa za OCR brzinu. Stvarna AMD GPU akceleracija bi zahtevala
# torch-directml (nije instaliran) + izmene u ocr_engine.py, ili prelazak na
# OCR engine koji stvarno koristi ONNX (npr. OnnxTR) - vidi analizu.
onnxruntime-directml>=1.20.0

# Image Processing
opencv-python>=4.9.0.80
numpy>=1.26.0
Pillow>=10.0.0

# PDF Support
PyMuPDF>=1.23.8

# Data Validation
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0

# WS6 (OPCIONO, EKSPERIMENTALNO) - TrOCR rukom pisan tekst kao "drugo
# mišljenje" (processors/handwriting_ocr.py). Potreban SAMO ako se
# Config.ENABLE_HANDWRITING_TROCR uključi (default False - servis normalno
# radi i bez ovog paketa instaliranog).
# transformers>=4.40.0

